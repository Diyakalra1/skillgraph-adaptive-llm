# TRL GRPO extras for Colab — loose pins (Colab already ships torch).
# Install in the GRPO cell only, after requirements-colab.txt + editable install.
trl>=0.11.4
transformers>=4.46.0
datasets>=3.1.0
accelerate>=1.0.1
peft>=0.13.0
